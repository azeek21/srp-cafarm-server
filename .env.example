# PostgreSQL connection (local instance). The database must already exist
# (e.g. `createdb cattle`); the app creates tables, not the database.
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/cattle

# Comma-separated list of allowed CORS origins (the client dev server).
CORS_ORIGINS=http://localhost:3000
