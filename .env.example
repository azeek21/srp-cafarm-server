# PostgreSQL connection (sync psycopg driver)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/cattle

# Comma-separated list of allowed CORS origins (client dev server)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
